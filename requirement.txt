#here pip install works( pip insatll -r requirements.txt)
streamlit
python-dotenv #reads key value from .env file and sets it as an environment variable 

# PDF reading
pypdf

# Hugging Face (choose local or API usage)
transformers
accelerate
torch
huggingface_hub

# LangChain + HF Inference API (use HuggingFaceEndpoint; avoids InferenceClient.post error)
langchain
langchain-community
langchain-classic
langchain-huggingface

#to insatll all package
#to insatll local package in current virtual environemt we an directly insatll setup.py (python setup.py insatlal) file
#the second way
-e . 